post_install() {
  systemctl daemon-reload
  systemctl enable --now omarchy-syncd.service
}

post_upgrade() {
  systemctl daemon-reload
  systemctl try-restart omarchy-syncd.service
}

pre_remove() {
  systemctl disable --now omarchy-syncd.service
}

post_remove() {
  systemctl daemon-reload
}
